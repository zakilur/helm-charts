apiVersion: v1
kind: Service
metadata:
  labels:
    run: {{ .Values.control.name }}
  name: {{ .Values.control.name }}
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - port: 50000
    protocol: TCP
    targetPort: 50000
  selector:
    run: {{ .Values.control.name }}
  type: ClusterIP
