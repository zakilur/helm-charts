{{ if .Values.jwt.secrets }}
{{- range $secrets := .Values.jwt.secrets }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secrets.name }}
  labels:
    chart: "{{ $.Chart.Name }}-{{ $.Chart.Version }}"
    release: "{{ $.Release.Name }}"
    heritage: "{{ $.Release.Service }}"
type: Opaque
data:
  {{- range $key, $value := $secrets }}
  {{- if and (ne $key "name") }}
  {{ base $key }}: {{ $value | b64enc }}
  {{- end }}
  {{- end  }}
{{- end }}
{{- end }}
