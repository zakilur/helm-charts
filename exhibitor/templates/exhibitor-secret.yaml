kind: Secret
apiVersion: v1
metadata:
  name: exhibitor
  namespace: {{ .Release.Namespace }}
type: Opaque 
stringData:
  credentials:  "com.netflix.exhibitor.s3.access-key-id={{ .Values.aws.access_key }}\ncom.netflix.exhibitor.s3.access-secret-key={{ .Values.aws.secret_key }}"

