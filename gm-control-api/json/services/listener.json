{
  "listener_key": "listener-{{.serviceName}}",
  "zone_key": "{{.zone}}",
  "name": "{{.serviceName}}",
  "ip": "0.0.0.0",
  "port": "{{ .port | default 8080 }}",
  "protocol": "http_auto",
  "domain_keys": ["domain-{{.serviceName}}"],
  "tracing_config": null
}
