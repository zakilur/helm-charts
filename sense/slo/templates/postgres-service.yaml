{{- if not .Values.postgres.rds.enabled }}
kind: Service
apiVersion: v1
metadata:
  name: postgres-slo
  namespace: {{ .Release.Namespace }}
  labels:
    gm.proxy: postgres-slo
spec:
  ports:
  - name: postgres
    port: 5432
  selector:
    gm.proxy: postgres-slo
  type: ClusterIP
{{- end }}
