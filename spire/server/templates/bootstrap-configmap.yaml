kind: ConfigMap
apiVersion: v1
metadata:
  name: server-bootstrap-script
  namespace: spire
data:
  bootstrap.sh: |-
{{ .Files.Get "config/bootstrap.sh" | indent 4 }}
  setup.sh: |-
{{ .Files.Get "config/setup.sh" | indent 4 }}