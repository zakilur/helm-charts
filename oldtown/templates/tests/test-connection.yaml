{{/* apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oldtown.fullname" . }}-test-connection"
  labels:
{{ include "oldtown.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "oldtown.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never */}}
